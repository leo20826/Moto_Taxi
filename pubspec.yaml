name: moto_taxi_app
description: App de calculadora de tarifas para moto taxi
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.1.0
  geolocator_android: ^4.5.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  shared_preferences: ^2.2.2
  wakelock_plus: ^1.1.4
  permission_handler: ^11.0.1
  intl: ^0.18.1
  cupertino_icons: ^1.0.2
  sqflite: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true